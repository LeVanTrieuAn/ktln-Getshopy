# Getshopy client

Tài liệu chạy toàn bộ hệ thống nằm tại [README ở thư mục gốc](../README.md).

Để chạy riêng Vite ngoài Docker, sao chép `.env.example` thành `.env`, cài dependency bằng `npm ci`, rồi chạy `npm run dev`.
