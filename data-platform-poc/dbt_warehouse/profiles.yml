dbt_warehouse:
  outputs:
    dev:
      type: clickhouse
      host: localhost
      port: 8123
      user: "{{ env_var('CLICKHOUSE_USER') }}"
      password: "{{ env_var('CLICKHOUSE_PASSWORD') }}"
      schema: default
      secure: False
      connect_timeout: 10
  target: dev